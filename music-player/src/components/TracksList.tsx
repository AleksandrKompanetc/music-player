import { Track } from '../types'

export function TrackList({
  tracks,
  selectedTrackId,
  onSelectTrack,
  loading,
  error,
}) {
  return (
    <ul>
      {tracks.map((track) => {
        
      })}
    </ul>

  )
}